package pl.clarin.chronocorpus.task.entity;

public enum TaskType {
        document, concordance, frequency,
        time_series, word_profile,dictionaries,
        quantity_analysis, geo_proper_names
}
